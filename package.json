{
  "name": "zibel",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.5.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "tsc --noEmit && tsc --noEmit -p apps/web && biome check . && vitest run && pnpm check:bundle",
    "check:bundle": "pnpm --filter @zibel/web build && ls apps/web/dist/assets/*.js > /dev/null && ! grep -l xmldom apps/web/dist/assets/*.js",
    "bench": "pnpm --filter @zibel/web build && wrangler d1 migrations apply zibel --local -c apps/edge/wrangler.jsonc --persist-to .wrangler/bench && node fixtures/agent-benchmarks/run.ts",
    "roundtrip": "pnpm --filter @zibel/web build && wrangler d1 migrations apply zibel --local -c apps/edge/wrangler.jsonc --persist-to .wrangler/roundtrip && node fixtures/roundtrip.ts",
    "dev": "pnpm --filter @zibel/web build && wrangler d1 migrations apply zibel --local -c apps/edge/wrangler.jsonc && wrangler dev -c apps/edge/wrangler.jsonc",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14",
    "@cloudflare/vitest-plugin": "1.2.3",
    "@cloudflare/workers-types": "^5.20260922.1",
    "@playwright/test": "1.63.0",
    "@vitest/runner": "4.1.11",
    "@vitest/snapshot": "4.1.11",
    "typescript": "5.9.3",
    "vitest": "4.1.11",
    "wrangler": "4.136.3"
  }
}
